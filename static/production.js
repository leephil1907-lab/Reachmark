let qualityData={duplicates:[],reviews:[]},projectData={projects:[],stages:[],currencies:{},today:''};
const verificationLabel=v=>({UNREVIEWED:'Not manually reviewed',WORKING:'Website working — manual review',LISTED_URL_UNAVAILABLE:'Listed URL unavailable — manual evidence',NO_SITE_FOUND:'No website found in manual research',INCONCLUSIVE:'Inconclusive'}[v]||'Not manually reviewed');
function qualityMatches(l){return (!$('#filter-location').value||l.city===$('#filter-location').value)&&(!$('#filter-category').value||l.category===$('#filter-category').value)&&(!$('#filter-contact').value||($('#filter-contact').value==='email'?!!l.email:$('#filter-contact').value==='phone'?!!l.phone:!l.email&&!l.phone))&&(!$('#filter-verification').value||(l.verification||'UNREVIEWED')===$('#filter-verification').value)}
function syncQualityFilters(){for(const [id,key,label] of [['filter-location','city','All locations'],['filter-category','category','All categories']]){const input=$('#'+id);if(!input)return;const previous=input.value;input.innerHTML=`<option value="">${label}</option>`+[...new Set(state.leads.map(l=>l[key]).filter(Boolean))].sort().map(v=>`<option value="${esc(v)}">${esc(v)}</option>`).join('');input.value=previous;}}
async function loadQuality(){try{qualityData=await api('/api/quality');const find=id=>state.leads.find(l=>l.id===id);$('#duplicate-results').innerHTML=qualityData.duplicates.length?qualityData.duplicates.map(p=>`<div class="duplicate-row"><strong>${esc(find(p.first)?.name)} / ${esc(find(p.second)?.name)}</strong><p class="small muted">${esc(p.reasons.join(' · '))}. A possible match, not a confirmed duplicate.</p><button class="text-link" data-rm-click="openLead" data-rm-arg="${p.first}">Review first record ↗</button> <button class="text-link" data-rm-click="openLead" data-rm-arg="${p.second}">Review second record ↗</button></div>`).join(''):'<p class="small muted">No candidates found with matching phone, email or name/location. This does not prove there are no duplicates.</p>';if(qualityData.truncated)$('#duplicate-results').insertAdjacentHTML('beforeend','<p class="notice">Candidate display is capped at 200 pairs and 30 records per shared identifier.</p>')}catch(e){toast(e.message,true)}}
async function renderLeadWork(){const l=selected;if(!l)return;$('#lead-documents').innerHTML=`<a href="/api/documents/audit/${l.id}.pdf">↓ Audit report PDF</a><button class="secondary" type="button" data-rm-click="projectFromLead">Create linked project / proposal</button>`;$('#review-last-seen').textContent='Record last imported / seen: '+(l.source_seen_at?new Date(l.source_seen_at).toLocaleString():'Not recorded')+' · URL checked: '+(l.checked_at?new Date(l.checked_at).toLocaleString():'Not checked');const form=$('#review-form');form.reset();try{const d=await api('/api/quality');if(selected?.id!==l.id)return;const review=d.reviews.find(r=>r.lead_id===l.id);if(review)for(const k of ['verification','note','evidence_url'])form.elements[k].value=review[k]||'';$('#review-time').textContent=review?'Manual review: '+new Date(review.reviewed_at).toLocaleString():'No manual verification recorded.';}catch(e){$('#review-time').textContent=e.message}}
$('#review-form').onsubmit=async e=>{e.preventDefault();if(!selected)return;const b=e.submitter;b.disabled=true;try{await api(`/api/leads/${selected.id}/review`,'POST',Object.fromEntries(new FormData(e.target)));await refresh();selected=state.leads.find(l=>l.id===selected.id);await renderLeadWork();toast('Evidence and manual review saved.')}catch(e){toast(e.message,true)}finally{b.disabled=false}};
async function projectFromLead(){const id=selected?.id;closeDrawer();if($('#lead-drawer').classList.contains('open'))return;await editProject(null,id)}
async function loadProjects(){try{projectData=await api('/api/projects');renderProjects()}catch(e){toast(e.message,true)}}
function renderProjects(){const q=$('#project-search').value.toLowerCase(),filter=$('#project-followup').value,today=projectData.today;const active=p=>!['Delivered','On hold'].includes(p.stage);const overdue=p=>active(p)&&p.due_date&&p.due_date<today;const rows=projectData.projects.filter(p=>(!q||(p.title+' '+p.next_action).toLowerCase().includes(q))&&(!filter||(filter==='overdue'?overdue(p):filter==='today'?active(p)&&p.due_date===today:!p.due_date)));$('#project-reminders').textContent=`${projectData.projects.filter(overdue).length} overdue · ${projectData.projects.filter(p=>active(p)&&p.due_date===today).length} due today · ${projectData.projects.length} saved projects. Reminders use UTC calendar dates.`;$('#project-board').innerHTML=projectData.stages.map(s=>`<section class="project-column"><h3>${esc(s)} <span>${rows.filter(p=>p.stage===s).length}</span></h3>${rows.filter(p=>p.stage===s).map(p=>`<article class="project-tile ${overdue(p)?'overdue':''}"><h4>${esc(p.title)}</h4><p>${esc(p.next_action||'Set a next action')}</p><small>${p.due_date?'Follow up '+esc(p.due_date):'No follow-up date'}${overdue(p)?' · overdue':''}</small><button class="text-link" data-rm-click="editProject" data-rm-arg="${p.id}">Open project ↗</button></article>`).join('')||'<p class="small muted">No projects here.</p>'}</section>`).join('')}
async function editProject(id=null,leadId=''){try{await loadProjects();const contracts=await api('/api/contracts');let clients=[];try{clients=(await api('/api/clients')).clients||[];}catch(e){}const p=projectData.projects.find(p=>p.id===id),f=$('#project-form');f.reset();f.elements.id.value=p?.id||'';f.elements.lead_id.innerHTML='<option value="">No linked business</option>'+state.leads.map(l=>`<option value="${l.id}">${esc(l.name)}</option>`).join('');f.elements.contract_id.innerHTML='<option value="">No linked contract</option>'+contracts.contracts.map(c=>`<option value="${c.id}">${esc(c.title)} · ${esc(c.client)}</option>`).join('');f.elements.stage.innerHTML=projectData.stages.map(s=>`<option>${esc(s)}</option>`).join('');f.elements.currency.innerHTML=Object.keys(projectData.currencies).map(c=>`<option>${c}</option>`).join('');if(p){for(const k of ['title','lead_id','contract_id','stage','next_action','due_date','scope','currency'])f.elements[k].value=p[k]||'';if(f.elements.client_email) f.elements.client_email.value=p.client_email||clients.find(c=>c.id===p.client_user_id)?.email||'';f.elements.quote.value=p.quote_minor===null?'':(p.quote_minor/(10**projectData.currencies[p.currency])).toFixed(projectData.currencies[p.currency])}else{f.elements.currency.value='USD';f.elements.lead_id.value=leadId;f.elements.title.value=leadId?(state.leads.find(l=>l.id===leadId)?.name||'')+' — website project':'';if(f.elements.client_email) f.elements.client_email.value='';}$('#project-documents').innerHTML=p?`<a href="/api/documents/proposal/${p.id}.pdf">↓ Draft proposal & quote PDF</a><a href="/api/documents/brief/${p.id}.pdf">↓ Project brief PDF</a>${p.lead_id?`<button class="text-link" data-rm-click="closeModalAndOpenLead" data-rm-json='${JSON.stringify(["project-modal", p.lead_id, "preview"])}'>Business preview ↗</button><button class="text-link" data-rm-click="closeModalAndOpenLead" data-rm-json='${JSON.stringify(["project-modal", p.lead_id, "compose"])}'>Review outreach ↗</button>`:''}${p.contract_id?`<a href="/api/documents/contract/${p.contract_id}.pdf">↓ Contract record PDF</a>`:''}<button class="danger-link" data-rm-click="deleteProject" data-rm-arg="${p.id}">Delete project</button>`:'<p class="small muted">Save the project to download branded documents. Blank prices remain “tailored quote”; nothing is sent automatically.</p>';openModal('project-modal');setTimeout(()=>f.elements.title.focus(),120)}catch(e){toast(e.message,true)}}
$('#project-form').onsubmit=async e=>{e.preventDefault();const b=e.submitter;b.disabled=true;try{const v=Object.fromEntries(new FormData(e.target)),id=v.id;delete v.id;await api('/api/projects'+(id?'/'+id:''),id?'PATCH':'POST',v);closeModal('project-modal');await loadProjects();navigate('projects');toast('Project and follow-up saved. No email was sent.')}catch(e){toast(e.message,true)}finally{b.disabled=false}};
async function deleteProject(id){if(!confirm('Delete this project? Linked leads and contracts will remain.'))return;try{await api('/api/projects/'+id,'DELETE');closeModal('project-modal');await loadProjects()}catch(e){toast(e.message,true)}}
async function ownerLogout(){try{const r=await fetch('/logout',{method:'POST',headers:{'X-CSRF-Token':document.querySelector('meta[name="csrf-token"]').content}});if(r.ok)location.href='/login';else toast('Unable to sign out. Reload and try again.',true)}catch(e){toast(e.message,true)}}
async function loadReadiness(){try{const d=await api('/api/operations/readiness');$('#ops-readiness').innerHTML='<dl>'+Object.entries(d).map(([k,v])=>`<dt>${esc(k)}</dt><dd>${esc(v)}</dd>`).join('')+'</dl>'}catch(e){$('#ops-readiness').textContent=e.message}}
syncQualityFilters();if(state.leads.length)renderLeads();if(location.hash==='#projects'){navigate('projects');loadProjects()}loadReadiness();
setInterval(()=>{if(!document.hidden&&$('#page-projects').classList.contains('active')&&!$('#project-modal').classList.contains('open'))loadProjects()},30000);
